language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mozdevtools-presentation' ]; then cd .. && eval "mv $currentfolder generator-mozdevtools-presentation" && cd generator-mozdevtools-presentation; fi

